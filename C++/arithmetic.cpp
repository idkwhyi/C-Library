#include <iostream>

  //arithmetic operator = return the result of a specific arithmetic operator (+ - * /)
int main() {

  int students = 6 - 5 + 6 * 2 - 6;

  // students = students + 1;
  // students+=1;
  // students++;

  // students = students - 1;
  // students-=1;
  // students--;

  // students = students * 2;
  // students*=2;

  // students = students / 2;
  // students/=2;

  // int remainder = students % 2;

  std::cout << students;

  return 0;
}