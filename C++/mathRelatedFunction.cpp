#include <iostream>
#include <cmath>

int main() {

  double x = 3.99;
  double y = 5; 
  double z;

  // z = std::max(x, y);  
  //  memberikan output nilai terbesar antara 2 variabel (tidak bisa lebih dari 2)
  // z = std::min(x, y);   
  //  memberikan output nilai terkecil antara 2 variabel (tidak bisa lebih dari 2)

  // z = pow(2, 3);  //perpangkatan (2 pangkat 3)
  // z = sqrt(9);    //akar (9 adalah kuadrat dari 3)
  // z = abs(-4);    //mengubah bentuk negatf manjadi positif (dari -3 menjadi 3)
  //z = round(x);    //untuk mengubah desimal ke angka terdekat (dari 3.14 menjadi 3, dari 3.99 menjadi 4)
  // z = ceil (x);   //untuk mengubah angka desimal ke angka di atasnya (dari 3.14 menjadi 4)
  // z = floor(x);   //untuk mengubah angka desimal ke angka di bawahnya (dari 3.99 menjadi 4)


  // UNTUK MENEMUKAN OPERASI MATH LEBIH LANJUT https://www.cplusplus.com/reference/cmath


  std::cout << z; 

  return 0;
}